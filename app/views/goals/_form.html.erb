
<%= form_for(@goal, html: {class: 'form-horizontal'}) do |p| %>
   <% if @goal.errors.any? %>
  <div id="error_explanation">
<h2><%= pluralize(@goal.errors.count, "error") %> prohibited this post from being saved:</h2>
    <ul>
      <% @goal.errors.full_messages.each do |message| %>
      <li><%= message %></li>
   <% end %>
    </ul>
  </div>
<% end %>

  <div class="field form-group">
    <%= p.label :name, class: 'col-sm-1 control-label' %>
    <div class="col-sm-11">
    <%= p.text_field :name, class: 'form-control' %>
    </div>
  </div>

  <div class="field form-group">
    <%= p.label :minutes, class: 'col-sm-1 control-label' %>
    <div class="col-sm-11">
      <%= p.text_field :minutes, class: 'form-control' %>
    </div>
  </div>

  <div class="actions form-group">
    <div class="col-sm-offset-1 col-sm-11 btn btn-primary">
      <%= p.submit btn_label %>
    </div>
  </div>
<% end %>
